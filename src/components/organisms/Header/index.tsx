import React from "react"
import { Heading } from "@/components/atoms/Heading";

export const Header: React.VFC = () => (
  <header className="header">
    <Heading level={1}>minista Template</Heading>
  </header>
)
